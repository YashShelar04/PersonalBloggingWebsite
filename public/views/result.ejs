<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search Results</title>
</head>
<body>
    <h1>Search Results</h1>
    <% if (blogs.length > 0) { %>
        <% blogs.forEach(function(blog) { %>
            <div>
                <h2><%= blog.title %></h2>
                <p><%= blog.content %></p>
                <p>Posted on: <%= blog.date_of_save.toISOString().slice(0, 10) %></p>
            </div>
        <% }); %>
    <% } else { %>
        <p>No results found.</p>
    <% } %>
</body>
</html>
